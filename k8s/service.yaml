apiVersion: v1
kind: Service
metadata:
  name: rag-app
  namespace: rag-system
spec:
  type: NodePort
  selector:
    app: rag-app
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080
    protocol: TCP
    name: http